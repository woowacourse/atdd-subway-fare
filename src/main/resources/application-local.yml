spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password:
  h2:
    console:
      enabled: true

handlebars:
  suffix: .html
  enabled: true

logging:
  level:
    root: TRACE
  config: classpath:log4j2.xml